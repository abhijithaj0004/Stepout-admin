part of 'pick_image_cubit.dart';

class PickImageState {
  XFile? selectedImg;
  

  PickImageState(this.selectedImg);
}

class PickImageInitial extends PickImageState {
  PickImageInitial(super.selectedImg);
}
